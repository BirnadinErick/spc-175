---
import { getEntry } from "astro:content";
import PageLayout from "../layouts/PageLayout.astro";

const facilityEntry = await getEntry("singles", "gallery");
const { Content } = await facilityEntry.render();
---

<PageLayout frontmatter={facilityEntry.data}>
    <Content />
</PageLayout>
