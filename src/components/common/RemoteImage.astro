---
const { src, alt, klass } = Astro.props;
---

<img
  src={src}
  alt={alt}
  class={klass
    ? `min-h-[256px] object-cover min-w-full w-full ${klass}`
    : "min-h-[256px] h-auto  object-cover min-w-full w-full"}
  loading="lazy"
  decoding="async"
/>
